
@helper Img(byte[] src,string classe, string alt) {

    
        var base64 = Convert.ToBase64String(src);
        var imgSrc = String.Format("data:image/gif;base64,{0}", base64);
      
   

        <img class="@classe" src="@imgSrc" alt="@alt">
}
